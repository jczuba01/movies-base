<div class="container mt-4">
  <h1><%= @genre.name %></h1>

  <div class="mt-3 d-flex gap-2">
  <%= link_to "Back to Movies", movies_path, class: "btn btn-secondary" %>
  <%= link_to "Edit", edit_genre_path(@genre), class: "btn btn-warning" %>
  <%= link_to "Genre List", genres_path, class: "btn btn-primary" %>
  </div>
</div>